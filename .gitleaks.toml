# Allowlist for Stage 0 finding 1 (2026-09-19).
# The only gitleaks hit in full history was the documented placeholder
# np_test_YOUR_KEY in public curl examples. That is not a credential.
title = "Takehome"

[allowlist]
description = "Documented placeholders and the public rule-set integrity seed (not an API credential)"
regexes = ['''np_test_YOUR_KEY''']
paths = ['''data/rules/signing.seed''']
